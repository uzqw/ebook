# Relay Plan: mobile-reflow

Read only the item named by `status.md`.

## 1. Complete and verify mobile adaptive reflow

- Audit the existing focused implementation and tests against every charter finish-line item.
- Correct only issues needed to satisfy that finish line.
- Run the required frontend verification suite.
- Update `status.md`, append `log.md`, and create one focused commit without the unrelated `README.md` change.
- If all acceptance items pass, mark the relay `DONE`; otherwise identify the smallest clear next independently verifiable plan item before handoff, or mark `BLOCKED` if an intervention condition applies.

**State:** ready for leg 1.
